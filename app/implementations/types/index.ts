/* A type that represents the type of the value that the "collection" state will be holding. */
export type Collection = null;

/* A type that represents the type of the value that the "problem" state will be holding */
export type Problem = {
    problem: string,
    answer: number,
};

/* A type that represents the type of the user input */
export type UserAnswer = number;